box: kauplan/review2.5

build:
  steps:
    - script:
        name: Build PDF
        code: |
          rake clean pdf
          cp Clean-mailSettings.pdf ${WERCKER_REPORT_ARTIFACTS_DIR}/
          upload_dropbox.sh Clean-mailSettings.pdf ${DROPBOX_TOKEN}
